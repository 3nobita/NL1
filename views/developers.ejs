<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= developer ? developer.name : 'Details' %></title>
</head>
<body>
  <div class="container">
    <% if (developer) { %>
      <h1><%= developer.name %> Details</h1>
      <div class="developer-details">
        <img src="<%= developer.logo %>" alt="Logo of <%= developer.name %>">
        <p><strong>Established:</strong> <%= developer.established %></p>
        <p><strong>Project:</strong> <%= developer.project %></p>
        <p><strong>Short Description:</strong> <%= developer.shortDescription %></p>
        <p><strong>Long Description:</strong> <%= developer.longDescription %></p>
        <p><strong>Ongoing Projects:</strong> <%= developer.ongoingProjects %></p>
        <p><strong>City Present:</strong> <%= developer.cityPresent %></p>
      </div>
    <% } else { %>
      <p>No developer found.</p>
    <% } %>

    <% if (details && details.length > 0) { %>
      <h2>Details</h2>
      <div class="detail">
        <% details.forEach(detail => { %>
          <h3><%= detail.name %></h3>
          <img src="<%= detail.imageUrl %>" alt="Image of <%= detail.name %>">
          <p><strong>By:</strong> <%= detail.by %></p>
          <p><strong>Location:</strong> <%= detail.location %></p>
          <p><strong>Price:</strong> <%= detail.price %></p>
          <p><strong>Status:</strong> <%= detail.status %></p>
        <% }) %>
      </div>
    <% } %>

    <a href="/">Back to Developers List</a>
  </div>
</body>
</html>
